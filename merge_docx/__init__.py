import sys

from .merge import merge_docx

"""
template = input('Template: ')
file = input('File to be merged: ')
destination = input('Destination: ') 

if template[-5:] != '.docx':
    sys.exit('The template file needs to be in .docx format.')

if file[-5:] != '.docx':
    sys.exit('The file to be merged in needs to be in .docx format.')

merge_docx(template, file, destination)
"""